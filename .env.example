# Optional local tooling only. Leave values empty in this tracked template.
# The default VS Code GitHub MCP server uses OAuth and does not require a PAT.
GITHUB_PERSONAL_ACCESS_TOKEN=

# Optional when a personal Context7 plan requires authenticated requests.
CONTEXT7_API_KEY=
